import endpoint from './endpoint.js';

const {
  actionCreators,
  middleware,
  reducer,
  selectors,
} = endpoint;

export {
  actionCreators,
  middleware,
  selectors,
};

export default middleware;
